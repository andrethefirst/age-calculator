import Mars from '../src/mars'

describe('Mars', () => {

  let mars;

  beforeEach(() => {
    mars = new Mars();
  });

  test('should correctly calculate users age to their mars age', => {
    
  })
})